name: Tests

on:
    push:
        branches:
            - test
    workflow_dispatch:  # Manual run
jobs:
    test:
        name: Run unit tests
        runs-on: ubuntu-latest
        steps:
            -   name: Checkout
                uses: actions/checkout@v5
                with:
                    fetch-depth: 0

            #install PHP dependencies
            -   name: Setup PHP
                uses: shivammathur/setup-php@v2
                with:
                    php-version: '8.4'
                    tools: composer:v2
                    extensions: mbstring

            -   name: Show loaded PHP extensions
                run: php -m

            -   name: Get composer cache directory
                id: composer-cache
                working-directory: ./htdocs
                run: echo "dir=$(composer config cache-files-dir)" >> $GITHUB_OUTPUT

            -   name: Cache composer dependencies
                uses: actions/cache@v4.2.4
                with:
                    path: ${{ steps.composer-cache.outputs.dir }}
                    key: ${{ runner.os }}-composer-${{ hashFiles('**/composer.lock') }}
                    restore-keys: ${{ runner.os }}-composer-

            -   name: Install Composer dependencies
                working-directory: ./htdocs
                run: composer install --no-progress --prefer-dist --optimize-autoloader

            -   name: Create config file
                working-directory: ./htdocs
                run: cp ./config/local.neon.ci ./config/local.neon

            -   name: Run tests
                working-directory: ./htdocs
                run: vendor/bin/tester -p phpdbg tests/Cases/Unit -s -C #vendor/bin/tester -s -p "$(which php)" --colors 1 tests/Cases/Unit

            -   name: Upload results to Codecov
                uses: codecov/codecov-action@v5
                with:
                    token: ${{ secrets.CODECOV_TOKEN }}
                    files: coverage.xml
                    fail_ci_if_error: false
